$kind: http-request
method: GET
url: "{{baseUrl}}/api/notif/unsent"
description: Get notifications that haven't been sent to local device yet. Requires auth.
examples: .resources/Get Unsent Notifications.resources/examples
order: 4000
events:
  - listen: post-response
    script:
      content: |-
        pm.test("Status code adalah 200", function () {
            pm.response.to.have.status(200);
        });

        pm.test("Response memiliki field success = true", function () {
            const json = pm.response.json();
            pm.expect(json.success).to.eql(true);
        });

        pm.test("Response memiliki field data berupa array", function () {
            const json = pm.response.json();
            pm.expect(json).to.have.property("data");
            pm.expect(json.data).to.be.an("array");
        });
